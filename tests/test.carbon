package ExplorerTest api;

// A syntax highlighter for carbon programming language in vscode!
fn Main() -> i32 {
    let introduction: String = "Hello! from Rohan Vashisht!";
    let age: i32 = 18;
    Print(introduction);
    Print("My age is: {0}", age);
    return 0;
}
